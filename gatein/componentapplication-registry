WORKING_DIR=$GATEIN_HOME
WORKING_TOMCAT=$GATEIN_TOMCAT
VERSION=$GATEIN_VERSION
cd $WORKING_DIR/component/application-registry
mvn clean install -DskipTests
cp -v $WORKING_DIR/component/application-registry/target/exo.portal.component.application-registry-$VERSION.jar $WORKING_TOMCAT/lib
