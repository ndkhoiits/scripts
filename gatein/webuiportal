WORKING_DIR=$GATEIN_HOME
WORKING_TOMCAT=$GATEIN_TOMCAT
VERSION=$GATEIN_VERSION
cd $WORKING_DIR/webui/portal
mvn clean install
cp -v  $WORKING_DIR/webui/portal/target/exo.portal.webui.portal-$VERSION.jar $WORKING_TOMCAT/lib/