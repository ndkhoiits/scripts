WORKING_DIR=$GATEIN_HOME
WORKING_TOMCAT=$GATEIN_TOMCAT
VERSION=$GATEIN_VERSION
cd $WORKING_DIR/component/scripting
mvn clean install -DskipTests
cp -v $WORKING_DIR/component/scripting/target/exo.portal.component.scripting-$VERSION.jar $WORKING_TOMCAT/lib
