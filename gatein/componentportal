WORKING_DIR=$GATEIN_HOME
WORKING_TOMCAT=$GATEIN_TOMCAT
VERSION=$GATEIN_VERSION
cd $WORKING_DIR/component/portal
mvn clean install -DskipTests
cp -v $WORKING_DIR/component/portal/target/exo.portal.component.portal-$VERSION.jar $WORKING_TOMCAT/lib
